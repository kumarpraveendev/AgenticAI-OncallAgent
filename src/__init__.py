"""agentic-oncall reference slice.

The differentiated layer from the decision record: the gated two-phase
diagnose->act orchestrator (ADR-0003), the action gate keyed on blast radius and
environment (ADR-0002), runbook grounding (ADR-0004), and full-context
escalation (ADR-0005). Pure standard library — no API keys, no network.
"""
__version__ = "0.1.0"
